name: Tracking Issue

on:
  workflow_run:
    workflows: ["Create Weekly Assignment Issue and Milestone"]
    types:
      - completed

permissions:
    issues: write
    contents: write
    actions: read

jobs:
    call-tracking-issue:
        uses: HUTECH-BMTT/workflow-templates/.github/workflows/tracking-issue.yaml@main
        secrets: inherit
